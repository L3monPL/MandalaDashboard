import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { NavModernComponent } from './nav-modern.component';

@NgModule({
  declarations: [NavModernComponent],
  imports: [CommonModule],
  exports: [NavModernComponent]
})
export class NavModernModule {}
